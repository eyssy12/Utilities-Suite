<controls:ViewControlBase x:Class="File.Organiser.UI.Views.AddTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:File.Organiser.UI.Views"
             xmlns:controls="clr-namespace:File.Organiser.UI.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <controls:ViewControlBase.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Styles/OrganiserStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ViewControlBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Grid.Row="0" Padding="16" materialDesign:ShadowAssist.ShadowDepth="Depth2" Mode="PrimaryMid" DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal">
                <!--<materialDesign:PackIcon Kind="Magnify" Opacity="1" Width="30" Height="30"/>
                <TextBlock Margin="8 0 0 0" FontSize="22">Return</TextBlock>-->

                <Button Style="{StaticResource OrganiserMaterialDesignFlatButton}" 
                        HorizontalAlignment="Stretch"
                        Click="Button_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="KeyboardBackspace" Opacity="1" Foreground="White" />
                        <TextBlock Margin="8 0 0 0" Foreground="White" >Return</TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
        </materialDesign:ColorZone>

        <StackPanel Grid.Row="1">
            <StackPanel VerticalAlignment="Bottom">
                <Button>
                    Add task
                </Button>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</controls:ViewControlBase>